<%= form_with(model: subscription) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.email_field :email, placeholder: "Введите ваш email", class:"A_FooterSubFormInput A_RegularText"  %>
  
    <%= form.button type: "submit", class: "A_FooterSubFormButton" do %>
      <%= image_tag "base/sub_arrow.svg", alt: "Submit" %>
    <% end %>
  </div>
<% end %>
