<%= form_with(model: [:admin, swap]) do |form| %>
  <% if swap.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(swap.errors.count, "error") %> prohibited this swap from being saved:</h2>

      <ul>
        <% swap.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  <%= form.label :swap_image, style: "display: block" %>
  <%= form.file_field :swap_image %>
</div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>



  <div>
    <%= form.submit %>
  </div>
<% end %>
