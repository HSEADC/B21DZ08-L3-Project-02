<p style="color: green"><%= notice %></p>

<%= render @plant %>

<%= link_to "New Comment", new_plant_comment_path(@plant) %>

<h2>Comments</h2>
<p>Yoy</p>
<%= render @plant.comments %>

<div>
  <%= link_to "Edit this plant", edit_plant_path(@plant) %> |
  <%= link_to "Back to plant", plants_path %>

  <%= button_to "Destroy this plant", @plant, method: :delete %>
</div>