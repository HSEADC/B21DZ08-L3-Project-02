
<% if plant.plant_image.present? %>
  <%= image_tag plant.plant_image.url, class: "M_SwapCardImg" %>
<% else %>
  <div></div>
<% end %>

  <div class="A_CardNamePadding A_AverageMediumText ">
  <%= plant.name %>
  </div>